# 以太坊 RPC（默认使用公共节点，生产环境建议用 Alchemy / Infura）
ETH_RPC_URL=https://ethereum.publicnode.com

# Etherscan API Key（免费注册：https://etherscan.io/register）
# 用于获取合约 ABI，没有则只输出原始 selector
ETHERSCAN_API_KEY=

# Anthropic API Key（可选）
# 有则自动调用 LLM 生成解释；没有则打印结构化 Prompt 供手动粘贴
ANTHROPIC_API_KEY=
